<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lgUCamp.catchMe.Mapper.UserMapper">
    <select id="getUserInfo" resultType="com.lgUCamp.catchMe.DTO.UserDTO">
        SELECT * FROM tbl_user
        WHERE USER_ID= #{user_id}
    </select>

    <insert id="saveUser" parameterType="com.lgUCamp.catchMe.DTO.UserDTO">
        INSERT
            INTO tbl_user
                (USER_ID, USER_PASS, USER_NAME, USER_BIRTH, USER_MAIL, USER_PHONE, USER_NICKNAME)
            VALUES(#{user_id}, #{user_pass}, #{user_name}, #{user_birth}, #{user_mail}, #{user_phone}, #{user_nickname});
    </insert>
</mapper>